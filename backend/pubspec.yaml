name: yogo_vital_backend
description: Dart Frog backend for YogoVitalApp (auth + Postgres)
version: 0.1.0
environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  dart_frog: ^1.2.6
  postgres: ^2.5.4
  dart_jsonwebtoken: ^2.0.1
  bcrypt: ^1.1.3
  mailer: ^6.2.7
  dotenv: any
  http: ^1.6.0

dev_dependencies:
  dart_frog_cli: ^1.2.11
