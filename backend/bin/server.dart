// This repository uses Dart Frog for the backend.
// Use the Dart Frog CLI to run the server during development:
// 1. Install CLI: `dart pub global activate dart_frog_cli`
// 2. From the backend folder run: `dart_frog dev`
//
// The old shelf-based server was replaced. Routes are under `routes/`.

void main() {
  print(
      'This backend uses Dart Frog. Run `dart_frog dev` from the backend folder.');
}
